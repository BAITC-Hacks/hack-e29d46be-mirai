> Финальный аудит исходных PDF и кода: [FINAL_AUDIT.md](FINAL_AUDIT.md).
> #40/#41/#42 уже в main e1dabc3, включая исправление #39.
> PR #45 (#43) — точность объяснений и входных ID; PR #46 (#44) — общий каталог launcher/UI.
> Совместная сборка: 200 Python-тестов + 8 subtests, 20 Node-тестов проходят.
> Остаются мерж согласованных PR и Windows-подтверждение Даниала (#37).
> Презентация 29 сентября. Сдача на платформе запланирована командой на 17:50.
> Ниже — исторические снимки; текущие статусы уточнять в GitHub.
>
> Предыдущая проверка после main f942863: [AUDIT-2026-09-23.md](AUDIT-2026-09-23.md).
> #32/#34/#38 смержены, #26/#35 закрыты как дубли, #13/#33 завершены.
> #36 готовится к ревью: исправленная проекция меняет состав кластеров, роли/рейтинг сохранены.
> #31 и #39 — UI/согласованность данных у Нурай; #37 ждёт UI-интеграции действий.
> Ниже сохранена история, а не текущий порядок мержа.

> Обновление от Мираса: #24 смержен. Сегодня (23 сентября) презентации нет,
> демо-день 29 сентября. До 18:00 продолжаем код, интерфейс и интеграцию;
> прежний план репетиций и фича-фриз в 17:00 отменены. #32 переводится на main.
> Даниал опубликовал исправления #29 в #23/#25/#27; требуется повторное ревью.

# Текущий статус ведущего miras-gpt (23 сентября, 15:51)

Мирас подтвердил мерж #17. #24 перенаправлен на main после разрешения истории squash,
без force-push; 31 тест + 8 subtests прошли. Подготовлено исправление #30 поверх #24,
которое включает интеграцию флагов из #26 и дополнительно исправляет экспорт метрик,
ранжирование resilience и отказ необязательных расчётов. **#26 отдельно не мержить
вместе с заменяющим его PR #32**: это перекрывающиеся изменения.

Порядок: #24 → PR #32 (после смены его базы на main).
Ассистент отдельно: #23 → #25 → #27, только после исправления блокеров из #29
и повторной проверки баз веток после squash. UI-запросы — #31.

На ветке #30: 43 теста + 8 subtests проходят в новом Python 3.12 venv;
полный прогон 2.2 секунды после прогрева, 2248 узлов, 91 кластер, топ-50.
Роли и пороги сохранены. `rapid_outflow` — близость дат; `fast_transit` — отдельный
строгий FIFO-показатель. Выгрузки пересчитаны. Windows и реальный LLM ещё не проверены.

Демо проводит Даниал на Windows, закрытие в 18:00. Подготовка ноутбука, сценарий
5 минут, схема и план — [DEMO.md](DEMO.md). Фича-фриз остаётся в 17:00.
Astra у агента разработки не определяет LLM внутри приложения.

Ниже — исторический снимок передачи в 15:15; его списки PR не являются текущим статусом.

---

# Передача ведущей роли: miras-claude → miras-gpt (15:15)

У Claude может закончиться лимит, поэтому с 15:15 **ведущий агент Мираса — `miras-gpt` (Codex)**.
Он забирает зону `miras-claude` целиком: ядро пайплайна, README, демо, ревью и интеграцию.
Claude остаётся резервом: включается, только если Мирас попросит.

## Что уже сделано (в main)
| Что | Где | Статус |
|---|---|---|
| Протокол агентов, ТЗ, контракты, план | `AGENTS.md`, `docs/CASE.md`, `docs/CONTRACTS.md`, `docs/PLAN.md` | ✅ |
| Пайплайн: метрики, роли, координаторы, приоритет, кластеры, раскладка, экспорт + самопроверка | `pipeline/` (кроме `extras.py`), `run.py` | ✅ ~2 с на данных |
| Выгрузки на реальных данных | `outputs/` (3 CSV + `graph.json`) | ✅ пересчитать после мержа extras |
| Мок для разработки | `shared/sample_graph.json` (`python shared/make_sample.py`) | ✅ |
| README (запуск, роли и пороги, ловушки данных, приоритет, ограничения, масштабирование) | `README.md` | ✅ разделы «Интерфейс» и «AI-ассистент» — заглушки |
| Сервер API + экран графа (Нурай) | `app/` | ✅ #18, #19 |

Роли на данных: peripheral 1 619 · terminal 281 · transit 248 · distributor 48 · consolidator 36 · coordinator 16.
Логика ролей и формула приоритета — в README и `pipeline/config.py` (все пороги в одном месте).

## Открытые PR на 15:15 (порядок мержа важен!)
| # | Что | Автор | Состояние |
|---|---|---|---|
| #20 | карточка клиента, AI-карточка, чат | Нурай | ✅ проверен — мержить |
| #17 | extras: временные признаки (#5) | miras-gpt | ✅ проверен — мержить |
| #24 | extras: циклы, маршруты, устойчивость (#6) | miras-gpt | ⚠️ **база — ветка #17**, мержить только после #17 (GitHub сам перенаправит на main); не проверен |
| #26 | интеграция extras в ядро: флаги без дублей, `sync_inflow`, `meta.metric_labels/flag_labels` | miras-claude | ✅ проверен с #17 — мержить после #17 |
| #23 | клиент LLM (#10) | Даниал | 🐞 **баг**: `os.getenv("LLM_MODEL", DEFAULT)` при `LLM_MODEL=` в `.env` даёт пустую модель → все вызовы молча None. Нужно `os.getenv("LLM_MODEL") or DEFAULT` (4 места). Комментарий в PR оставлен |
| #25 | `node_card` (#11) | Даниал | не проверен |
| #27 | `ask` с инструментами по графу (#12) | Даниал | ⚠️ **база — ветка #25**, мержить только после #25; не проверен |

Рекомендуемый порядок: **#20 → #17 → #24 → #26 → #23 (после фикса) → #25 → #27**.
⚠️ Урок #16: PR, стоящий на чужой ветке, мержить **только после** того, как смержена его база,
и проверять, что после мержа он указывает на `main`.

## Что осталось сделать
1. **[сейчас] Ревью #24, #25, #27** — собрать все открытые ветки в одну временную копию main
   (`git worktree add`), прогнать `python -m pytest -q app/tests tests`, `python run.py`, открыть
   http://localhost:8000, проверить: поиск gid → фокус и связи, карточка, AI-карточка, чат отвечает по данным.
2. **[после мержа extras] Пересчитать выгрузки**: `python run.py --no-serve` и `python shared/make_sample.py`,
   закоммитить `outputs/` и `shared/sample_graph.json` (в `graph.json` появятся флаги и `extras.cycles/resilience`).
3. **[16:10] Интеграция**: чистый клон в новую папку → `pip install -r requirements.txt` → `python run.py` →
   всё работает без ручных шагов. Проверить must-have из `docs/CASE.md` по пунктам.
4. **README**: дописать разделы «Интерфейс» и «AI-ассистент» (что умеют, как спросить), методологию extras
   (из `docs/methodology/`), обновить цифры ролей/флагов после extras.
5. **Демо (#13)**: `docs/demo/script.md` — 5 минут: проблема (1 мин) → живой `python run.py` → топ-лист →
   разбор 2–3 узлов (координатор, консолидатор, узел 4-го колена — показать, что он не terminal) → вопрос ассистенту →
   ограничения. `docs/demo/diagram.md` — схема «данные → метрики → роли → интерфейс» (1 слайд).
6. **[17:00] Фича-фриз**, дальше только баги. **[17:50] Сдача**: всё в main, выгрузки актуальны, README полный.

## Известные нюансы
- gid 18-значные → в `graph.json`, API и интерфейсе **строки** (иначе JS округлит).
- `run.py` запускает сервер, только если есть `app/server.py`; флаги: `--no-serve`, `--skip-pipeline`, `--port`.
- Extras подключаются в `pipeline/main.py::_extras` через try/except; во флаги попадают только `config.EXTRA_FLAGS`.
- Для тестов `app/tests` нужны `pytest` и `httpx` (не в requirements — dev-зависимости).
