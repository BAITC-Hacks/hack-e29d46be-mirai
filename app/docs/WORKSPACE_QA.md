# Проверка рабочего места Mirai — 23 сентября 2026

## Снимок и объём

Изменения только в app/. Интеграционный снимок main: `f942863d44cff1056370c13d29e2af86bfbaf388`.
Данные из объединённого #32: 2248 клиентов, 3119 рёбер; blob graph.json
`d164727c2319b4fce1f481144a0f48c8c4d2741b`. Ассистент взят из того же main,
включая новые actions.py/questions.py. Файлы pipeline/assistant/outputs не изменяются этим PR.

## Автоматические проверки

- Python app: 13 тестов, включая прежние 8 тестов сервера.
- Совместно app + текущие assistant/tests: 126 passed. Временное окружение Python 3.12,
  FastAPI/TestClient, pytest, httpx и openai; вызовы провайдеров подменяются тестами.
  Первая расширенная проверка выявила отсутствие SDK openai в тестовом окружении;
  после установки тестовой зависимости повторный прогон прошёл.
- Node: 10 тестов graph-model.cjs. Проверены кластер вне топа, пустое выделение эго-сети,
  устойчивые ID, направление путей, общие получатели за пределами топ-20,
  точность маленьких чисел, локальная раскладка, SVG и замыкающее ребро цикла при возврате.
- Стандартный launcher `python run.py --skip-pipeline --port 8768` запускает сервер;
  `/` и `/api/graph?workspace=true` вернули 200, 2248 клиентов / 3119 связей.
- HTTP + настоящий граф + настоящий assistant с LLM_PROVIDER=none: вопрос
  «Кто ему отправлял?» с явным контекстом возвращает 9 входящих; путь
  100000004015047100 → 100000001616816100 передаёт проверенное действие path;
  общий вопрос о приоритете возвращает 10 ссылок; карточка возвращается из шаблона.
- Обычный /api/graph сохраняет контракт. Старый graph_version блокируется ответом 409
  до обращения к ассистенту. Повреждённый JSON не меняет версию последнего валидного графа.

## Браузерная проверка

Встроенный Chromium на macOS, 1280×720 и 1440×900:

1. Поиск 100000004015047100 открывает 10 узлов / 12 связей, компактную схему и карточку.
2. Узел перетаскивается; на итоговом кадре линии соединены с новой позицией. Обновление
   тех же данных сохраняет положение и камеру. Клик по фону и повторное обновление
   сохраняют эго-сеть. Код отключает hideEdgesOnViewport и textureOnViewport.
3. Кластер 27 показывает все 15 узлов / 15 связей. Кластер 72 показывает изолированный
   узел; клик по нему сохраняет кластер и состав сцены. «Назад» возвращает выбор.
4. Явный контекст в чате, ответ о входящих, запрос пути и кнопка «Показать путь»
   проходят через HTTP и выделяют связь на карте.
5. Сравнение A/B показывает суммы/роли/приоритет, направленный путь; закладка сохраняется
   в списке. «Больше карты» скрывает боковые панели и позволяет вернуть их.
6. Клик по ребру 100000001616816100 → 100000001641789100 открывает 17 826 ₸ / 1 перевод.
7. Кнопка SVG выполняет генерацию без ошибки. Генератор проверен тестом на векторную
   геометрию, экранирование, встречные рёбра и петлю. Событие скачивания во встроенном
   браузере не было подтверждено инструментом; сохранение файла проверить в Chrome/Edge.

## Что остаётся проверить на Windows перед демо

Даниал / #37: запустить общий main после merge app-PR с LLM_PROVIDER=none; повторить
сценарий выше в Chrome/Edge при 1280×720 и 1440×900, масштаб ОС 100% и 125%.
На полном графе непрерывно pan/zoom и двигать узел: рёбра должны быть видны во время
жеста, а не только после отпускания. Зафиксировать FPS/время кадра и железо, если нужно
заявлять плавность численно. 60 FPS этим отчётом не подтверждаются.

Скачать SVG, открыть отдельно и увеличить до 400%; стрелки/подписи остаются векторами.
Проверить reduced-motion в настройках ОС, копирование gid и сохранение закладки после
перезапуска браузера. Ручные координаты сохраняются только в памяти вкладки, не в закладке.

Мирас / #36: проверить появление optional evidence_items/priority_components/rank_stability
после пересчёта, согласовать MIRAI_OUTPUT_DIR с launcher --out; корневой run.py этим PR не меняется.

Живой LLM, Windows/GPU и полный пересчёт сырых parquet здесь не тестировались.
Интерактивная карта остаётся Canvas; SVG используется для экспорта. Дневной фильтр,
полноценный SVG-renderer и новые расчёты ранга не объявляются выполненными в app-PR.

## Финальная интеграция с #40

После публикации #41 main обновился до e94f11d. Повторно проверен новый graph.json
(blob 35da1d1, 5.88 MB): optional объяснения, вклады и диапазон ранга отображаются;
техническое разложение скрыто под «Как рассчитан приоритет». Подсветка evidence_items
показывает реальные входящие; период 2026-07-01 — 2026-07-31 виден в шапке.
Однократный локальный TestClient замер: graph+JSON decode 278 мс, fallback ask вместе
с deepcopy графа 88 мс. Это диагностический замер сервера на этом Mac, не benchmark
браузера/Windows и не обещание FPS. Путь через HTTP и actions на новом графе прошёл.
